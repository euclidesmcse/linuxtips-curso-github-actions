name: primeiro workflow Linuxtips

on: 
    workflow_dispatch

jobs:
    giropops:
        runs-on: ubuntu-latest
        steps: 
            - name: Confere  versai do ubuntu
              run: cat /etc/os-release

            - name: Exibir uma mensagem
              run: echo "Estou fazendo curso na Linux"
